import { SidebarLinkDirective } from './sidebar-link.directive';

describe('SidebarLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarLinkDirective();
    expect(directive).toBeTruthy();
  });
});
